# Generated by Jarvis v{{JARVIS_VERSION}} on {{ISO_DATE}}
# feature:data table:users-v2 template:users_v2@{{TEMPLATE_HASH}}
#
# Composite-key tenant isolation pattern from CloudMortgage.
# DO NOT REMOVE the lifecycle block — the 2026-01-18 incident at
# CloudMortgage destroyed tables when this was missing.

resource "aws_dynamodb_table" "users" {
  name         = "${var.app_name}-users-v2"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "tenantId"
  range_key    = "email"

  attribute { name = "tenantId" type = "S" }
  attribute { name = "email"    type = "S" }
  attribute { name = "userId"   type = "S" }

  global_secondary_index {
    name            = "userId-index"
    hash_key        = "userId"
    projection_type = "ALL"
  }

  point_in_time_recovery { enabled = true }
  deletion_protection_enabled = true

  server_side_encryption { enabled = true }

  lifecycle {
    prevent_destroy = true
    ignore_changes  = [attribute, global_secondary_index]
  }

  tags = merge(local.common_tags, { Table = "users-v2" })
}
