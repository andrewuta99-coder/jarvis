# Generated by Jarvis v{{JARVIS_VERSION}} on {{ISO_DATE}}
# feature:data table:customers template:customers@{{TEMPLATE_HASH}}

resource "aws_dynamodb_table" "customers" {
  name         = "${var.app_name}-customers"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "userId"

  attribute { name = "userId"             type = "S" }
  attribute { name = "stripe_customer_id" type = "S" }

  global_secondary_index {
    name            = "stripe_customer-index"
    hash_key        = "stripe_customer_id"
    projection_type = "ALL"
  }

  point_in_time_recovery { enabled = true }
  deletion_protection_enabled = true
  server_side_encryption { enabled = true }

  lifecycle {
    prevent_destroy = true
    ignore_changes  = [attribute, global_secondary_index]
  }

  tags = merge(local.common_tags, { Table = "customers" })
}
