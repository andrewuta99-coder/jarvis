import React from "react";
import { useAuth } from "../shared/contexts/AuthContext";

export default function Dashboard() {
  const { user } = useAuth();
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">Welcome, {user?.email}</h1>
      <p className="text-gray-500">Your account is set up.</p>

      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-6">
        <a href="/chat"
           className="block p-4 rounded-lg border border-gray-200 dark:border-gray-800 hover:bg-gray-50 dark:hover:bg-gray-900 transition">
          <h3 className="font-medium">AI Chat</h3>
          <p className="text-sm text-gray-500">Talk to your AI assistant</p>
        </a>
        <a href="/billing"
           className="block p-4 rounded-lg border border-gray-200 dark:border-gray-800 hover:bg-gray-50 dark:hover:bg-gray-900 transition">
          <h3 className="font-medium">Billing</h3>
          <p className="text-sm text-gray-500">Manage your subscription</p>
        </a>
      </div>
    </div>
  );
}
